[project]
name = "learn-adk"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi",
    "uvicorn",
    "vertexai",
    "google-cloud-aiplatform[adk,agent-engines]",
    "google-genai",
    "pydantic",
    "python-dotenv",
]
